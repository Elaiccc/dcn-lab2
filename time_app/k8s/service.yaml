# Ziyi Liu - DCN Lab 2
apiVersion: v1
kind: Service
metadata:
  name: sample-time-app
  namespace: dcn-lab2
spec:
  type: NodePort
  selector:
    app: sample-time-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
